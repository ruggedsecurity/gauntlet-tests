library identifier: 'pipeline-library@master', retriever: modernSCM(
    [$class: 'GitSCMSource', 
    credentialsId: '', 
    id: 'pipeline-library', 
    remote: 'https://github.com/devopspatterns/pipeline-library.git', 
    traits: [[$class: 'jenkins.plugins.git.traits.BranchDiscoveryTrait']]
]) 

podTemplate(label: 'gauntlt-pod', 
    containers: [containerTemplate(name: 'gauntlt', image: 'gauntlt/gauntlt', ttyEnabled: true)],
    volumes: [hostPathVolume(hostPath: '.', mountPath: '/working')])
{
    node('gauntlt-pod') {
        container('gauntlt') {
            stage('Get Tests') {
                checkout scm
                stage('Test project') {
                    sh '--help'
                }
            }
        }
    }
}
